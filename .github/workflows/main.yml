name: Ping Render Service v4

on:
  schedule:
    # 10분마다 실행 (UTC 시간 기준)
    - cron: "*/7 * * * *"
  # 수동으로 실행 가능하게 함
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Service
        run: curl https://teamproject-odpg.onrender.com/api/check_admin_login/
