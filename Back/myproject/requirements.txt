# Django 기본 패키지
Django>=4.2.0,<5.0.0

# REST Framework
djangorestframework==3.16.0

# JWT 인증
djangorestframework-simplejwt==5.5.1

# CORS 처리
django-cors-headers>=4.0.0

# 환경 변수 관리
django-environ>=0.11.0
cryptography>=42.0.0

# PostgreSQL 데이터베이스
psycopg2-binary>=2.9.0

# WebSocket 지원 (Channels)
channels>=4.0.0
channels-redis>=4.1.0
daphne>=4.0.0
uvicorn[standard]

# Google OAuth
google-auth-oauthlib==1.2.3
google-auth==2.41.1

# HTTP 요청
requests==2.32.3

# 스케줄링 작업
django-apscheduler>=0.6.2

black>=23.0.0

# Redis 캐시 백엔드 (Upstash 연동 필수 패키지)
django-redis>=5.4.0

# 엑셀 파일 처리
openpyxl==3.1.5

# 배포용 웹 서버 및 정적 파일 관리
gunicorn>=21.2.0
whitenoise>=6.5.0
